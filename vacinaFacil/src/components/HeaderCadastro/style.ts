import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    backgroundColor: "#13293D",
    flexDirection: "row",
    width: "100%",
    height:100,
    alignItems:"center",
  },
  logo: {
    height: 60,
    width: 60,
    top:15,
  },
});
