import { StyleSheet} from "react-native";

export const styles = StyleSheet.create({
 styleButton: {
    width: "80%",
   // height: 37,
    borderRadius: 12,
     alignItems: "center",
   justifyContent: "center",
   paddingVertical: 4,
}, 

   text: {
    color: "#fff",
     fontSize: 30, 
   },
});
