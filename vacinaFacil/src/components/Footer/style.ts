import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    width: 100,
    alignItems: "center",
    marginTop: 12,
  },
  textFocus: { color: "#fff", fontSize: 15 },

  textUnFocus: { color: "#999", fontSize: 12 },
});
