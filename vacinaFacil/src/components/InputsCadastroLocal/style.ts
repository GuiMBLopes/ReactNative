import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxInputText: {
    width: "100%",
  },

  inputText: {
    backgroundColor: "#fff",
    width: "100%",
    borderRadius: 5,
    paddingHorizontal: 10,
    fontSize: 14,
    height: 50,
  },

  text: {
    color: "#E8F1F2",
    fontSize: 18,
  },
});
